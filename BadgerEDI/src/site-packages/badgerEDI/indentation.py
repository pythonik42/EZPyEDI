'''
Created on Aug 1, 2011

@author: jknaus
'''
    
#===============================================================================
#        
# Indentation helper class
#        
#===============================================================================
class Indentation(object):
    level = 0

    def __init__(self, indentString, noIndentString):
        if indentString:
            self.levelIndent = indentString
        else: 
            self.levelIndent = '    '
        if noIndentString:
            self.noIndent = noIndentString
        else:
            self.noIndent = ''
        self.level = 0
        self.indent = self.noIndent

    def increment(self):
        if self.level:
            self.level += 1
        else:
            self.level = 1
        self.indent = self.noIndent+self.level*self.levelIndent
        
    def decrement(self):
        if self.level:
            self.level -= 1
            if self.level:
                self.indent = self.noIndent+self.level*self.levelIndent
            else:
                self.indent = self.noIndent
        else:
            print 'Error: Level already at 0 in decIndent()!'
    
    def setLevel(self, level):
        self.level = level
        if self.level:
            self.indent = self.noIndent+self.level*self.levelIndent
        else:
            self.indent = self.noIndent
            
    def __repr__(self):
        return self.indent