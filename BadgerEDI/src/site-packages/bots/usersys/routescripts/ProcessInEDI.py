'''
Created on Jun 3, 2011

@author: jknaus
'''
import bots.transform as transform
from bots.botsconfig import *

from badgerEDI import eLog

def postincommunication(routedict): 
    
#    eLog.debug('Routescript.postincommunication:',routedict['idroute'],' seq:',routedict['seq'])
#    if routedict['seq'] <= 100:    #(passthrough) change status of incoming files to outgoing for seq nums <= 100
#        eLog.debug('Stopped route (seq<=100) processing and pass doc through to out channel')
#        transform.addinfo(change={'status':MERGED},
#                          where={'status':FILEIN,'idroute':routedict['idroute']})
#    else:
#        eLog.debug('Unchanged routedict[] = %s' , routedict)
    pass

    