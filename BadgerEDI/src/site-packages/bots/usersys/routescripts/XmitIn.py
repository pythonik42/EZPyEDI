'''
Created on Jun 3, 2011

@author: jknaus
'''

import bots.transform as transform
from bots.botsconfig import *

def postincommunication(routedict):
    print 'XmitIn.postincommunication seq=%s' % (routedict['seq'])
    if routedict['seq'] in [800]:    #(passthrough) change status of incoming files to outgoing (do not translate)
        transform.addinfo(change={'status':MERGED},
                            where={'status':FILEIN,'idroute':routedict['idroute']})

