"""
Sets up Badger EDI Logging for this package

Modules can import the BADGER EDI logger into their namespace by importing badgerEDI.eLog
as one of the first statements in their module file like this: 

    from badgerEDI import eLog
    
@author: jknaus
"""

import logging

logging.addLevelName(logging.ERROR, 'ERR')
logging.addLevelName(logging.WARNING, 'WRN')
logging.addLevelName(logging.INFO, 'INF')
logging.addLevelName(logging.CRITICAL, 'CRT')
logging.addLevelName(logging.DEBUG, 'DBG')
logging.addLevelName(logging.NOTSET, 'INF')

eLog = logging.getLogger('bots')

# for testing
#eLog.setLevel(logging.DEBUG)