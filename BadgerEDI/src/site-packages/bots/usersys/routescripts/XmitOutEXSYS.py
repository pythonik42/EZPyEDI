'''
Created on Dec 2, 2011

@author: jknaus
'''

import bots.transform as transform
from bots.botsconfig import *

def postincommunication(routedict):
    print 'XmitOutEXSYS.postincommunication seq=%s' % (routedict['seq'])
    if routedict['seq'] in [900]:    #(passthrough) change status of incoming files to outgoing (do not translate)
        transform.addinfo(change={'status':MERGED},
                            where={'status':FILEIN,'idroute':routedict['idroute']})

